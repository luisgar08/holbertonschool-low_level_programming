#include <stdio.h>
/**
 * main - enterpoint
 * Return: zero
 */
int main(void)
{
char letter = 'a'
  while (letter <= 122)
    {
     putchar(letter);
     letter++;
    }
 putchar('\n');
return 0;
}
