#include <stdio.h>
#include "main.h"
/**
 *add - print if the variable is lower
 *@x: variable
 *@y: variable
 *Description: print the result of a add
 *Return: operation
 */
void print_to_98(int n)
{
  int x;
  for (x = n; n <= 98; n++ )
    {
      printf("%d, ", x);
    }
return (0);
}
