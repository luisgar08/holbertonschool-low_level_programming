#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 *add - print if the variable is lower
 *@c: variable
 *Description: print if the variable c is lower
 *Return: 1 or 0
 */
int add(int x, int y)
{
  int o;
  o = x + y;
  return (o);
}
