#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 *add - print if the variable is lower
 *@xy: variable
 *Description: print the result of a add
 *Return: operation 
 */
int add(int x, int y)
{
int o;
o = x + y;
return (o);
}
