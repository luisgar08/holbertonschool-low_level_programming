#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 *print_alphabet_x10 - print 10 time the alphabet
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
